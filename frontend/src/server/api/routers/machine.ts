import { z } from "zod";

import {
  createTRPCRouter,
  protectedProcedure,
  publicProcedure,
} from "@/server/api/trpc";
import { Machine } from "@/types";

export const machineRouter = createTRPCRouter({});
