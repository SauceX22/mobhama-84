import { z } from "zod";

import {
  createTRPCRouter,
  protectedProcedure,
  publicProcedure,
} from "@/server/api/trpc";
import { Project } from "@/types";

export const projectRouter = createTRPCRouter({});
